module.exports = {
    // Specify the test environment
    testEnvironment: 'node',
  
    // Define the glob pattern to match test files
    testMatch: ['<rootDir>/tests/jest.index.js'],
  
  };